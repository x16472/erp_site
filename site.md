# 網站資訊架構

## 對外官方網站

`index.html` 面向家長與訪客，內容包含教育理念、孩子的一日作息、公開園務規模、教職團隊及參觀導引。教職團隊只顯示姓名、部門、職位與特質，並可使用部門下拉選單篩選；照片從 `static/staff` 載入。網站文字與主題由 `dbo.Frobel_WebSettings` 提供。

## 員工入口

`employee-login.html` 提供「上班打卡並進入」與「不打卡進入系統」。兩種方式都會查核員工在職狀態；不打卡模式只查詢當日最後打卡，不新增出勤紀錄。

`home.html` 是全體員工工作台，集中呈現園務摘要、班級概況、公告、知識搜尋與當日打卡狀態。未打卡時提供補打卡；打卡下班會新增下班紀錄，離開系統則只結束工作階段。

`sales.html` 用於提交每日營運紀錄；`exam.html` 可分類搜尋職掌、SOP 與教師手冊，並提供資料庫題庫；`game.html` 提供 YouTube 直播與影片網址轉換播放。

營運蒐集資料包含紀錄日期、園區、分類、數量與說明。說明欄不得輸入幼兒姓名、證件、電話、地址或其他可辨識個人的資訊。

## MIS 管理中心

`mis.html` 必須先通過員工入口，再以 `.env` 管理帳號登入。登入後可：

- 切換官方網站主題並調整首頁文字與公告。
- 讀取 SQL Server 實際資料表目錄。
- 分頁查閱經後端遮罩的資料內容。
- 透過「待審佇列」依員工或事件類別檢視待審營運紀錄。
- 重新同步 `data/exam` 內的教育 Word 文件。

`staff.html` 同樣使用 MIS 管理帳密登入，是唯一提供員工主檔新增、更新、停用與照片上傳的頁面，並可依日期與員工篩選完整出缺勤紀錄。新增後的員工編號主鍵會鎖定，不提供變更功能；名冊固定依員工編號排序。

MIS 管理中心無法從介面編輯既有園務資料表，也不能提交自訂 SQL。

## 導覽與品牌

官方網站與員工入口採不同的使用情境，但維持相同色彩、字型、按鈕與品牌圖像。所有圖像皆從 `static` 載入；桌機、平板與手機版面均有響應式調整。
