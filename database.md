# SQL Server 串接說明

## 連線設定

`data.py` 只會讀取專案既有的 `.env`：

- `DatabaseIP`
- `DatabasePort`
- `DatabaseName`
- `DatabaseUser`
- `DatabasePassword`
- `DatabaseDriver`（未設定時使用 `ODBC Driver 17 for SQL Server`）

建議正式環境使用只有 `SELECT` 權限的專用帳號，不要使用 `sa` 或資料庫擁有者帳號。

## 前端使用的資料範圍

網站以 `ALLtable` 作為主要查閱入口，並允許選取其他使用者資料表。查閱 API 會先從 SQL Server 中繼資料確認資料表名稱，再建立唯讀分頁查詢，因此前端不能注入任意資料表名稱或 SQL。

以下資料用於福祿貝爾園務總覽：

- `ALLtable`：僅依服務部門、分校代號進行群組計數。
- `分校資料`：只使用分校代號與簡稱。
- `職務類別`：只使用代號、職務類別與彙總人數。
- `學籍資料`：只讀取幼兒學籍總筆數。
- `班別名稱`：使用班級代號、名稱與園區代號。
- `搭交通車況`：只讀取接送服務總筆數。
- `繳費類別`、`繳費項目`、`繳費班別`、`繳費記錄`、`繳費明細`、`薪資表`：園務總覽只讀取總筆數。

幼兒、家長及教職員姓名、編號、生日等個人資料會部分遮罩；證件、地址、電話、接送聯絡、銀行、薪資、托育費用、津貼、保險及所得稅等受限制資料一律在後端替換為遮罩符號。薪資、銀行、健保、勞保、其他收入與異動金額資料表的所有欄位都以受限制資料處理。

## 正式上線前

1. 建立網站專用唯讀資料庫帳號。
2. 限制網站主機可連線的來源 IP。
3. 於反向代理啟用 HTTPS、登入驗證與工作階段逾時。
4. 為 API 加入角色授權及存取日誌。
5. 以園所實際的兒少資料分類政策覆核目前欄位白名單。
